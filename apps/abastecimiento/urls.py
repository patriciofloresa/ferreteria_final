from django.urls import path

from . import views

app_name = "abastecimiento"

urlpatterns = [
    # path('pedido/list', views.PedidoListView.as_view(), name='pedido_list'),
    # path('pedido/add', views.PedidoAddView.as_view(), name='pedido_add'),
    # path('pedido/edit/<int:pk>/', views.PedidoEditView.as_view(), name='pedido_edit'),
    # path('pedido/delete/<int:pk>/', views.PedidoDeleteView.as_view(), name='pedido_delete'),
    # path('pedido/ver/<int:pk>/', views.PedidoShowView.as_view(), name='pedido_detail'),
    # path('pedido/list_pendientes', views.PedidoListPendientesView.as_view(), name='pedido_list_pendientes'),
]
